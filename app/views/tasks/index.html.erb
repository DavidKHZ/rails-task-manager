<h1>Tasks</h1>

<% if (@tasks.length > 0) %>
  <ul>
  <% @tasks.each do |task| %>
    <li><%= task.title %></li>
  <% end %>
  </ul>
<% else %>
  <%= "No task to be displayed at the moment" %>
<% end %>
